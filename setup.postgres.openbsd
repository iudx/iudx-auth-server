#!/bin/sh

cd /var/postgresql

initdb -D /var/postgresql/data -U postgres --auth=md5 --pwprompt --encoding=UTF-8 --pwfile=db.password

export PGPASSWORD=`cat db.password`
psql -U postgres < postgres.sql
